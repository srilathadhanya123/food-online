    
<div class="row">

    
        @foreach(var item in Model)
{
    <div class="col-sm-4">   

        <div class="card">
  <img src="~/@item.Images">
  <div class="container">
  <div>@item.Itemid</div>
   <div>@item.Itemname</div>
   <div>@item.Price</div>
   <div>@item.Itemdesc</div>
   <a asp-action="Myorders"  asp-route-myitemid=@item.Itemid class="btn btn-info">Order Now</a>
  </div>         
  </div>
   </div>
  }
         
 </div>

  <div>
        Total Price @ViewData["total"]
    </div>

    @ViewData["a"]
    
